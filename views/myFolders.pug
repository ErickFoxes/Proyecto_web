extends layout

include nav/sessionNav

block content
    section.folders
        include sideBar/mFoldersSideBar
        article(class='myFolders')
            h3  #{title}
            //Lo que se agregue aqui me imagino sera dinamico pues se pondran y se quitaran carpetas xd
            p Contenido del folder
        form.register(action='/users/upload', method='POST'/*, enctype='multipart/form-data'*/)
            if locals.message
                img(src='#{message}', alt='')
            br
            |Selecciona la imagen
            input(type="file", name='photo')
            br
            input(type='submit', value='Guardar')






    